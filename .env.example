# Stylebase always binds to 127.0.0.1.
STYLEBASE_PORT=4177

# Optional: pin a Codex model. Leave blank to use the signed-in Codex default.
# STYLEBASE_CODEX_MODEL=

# Optional: override the executable when `codex` is not on PATH.
# STYLEBASE_CODEX_EXECUTABLE=codex
